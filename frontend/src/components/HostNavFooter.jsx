import React from 'react';
import UserNavFooter from './UserNavFooter';

const HostNavFooter = () => {
    const hostNavItems = [
        // Placeholder for host-specific navigation
    ];

    return <UserNavFooter navItems={hostNavItems} />;
};

export default HostNavFooter;
